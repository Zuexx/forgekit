# Legacy API Docs

The canonical API documentation lives in `../../docs/api/`.

This directory is retained as a legacy mirror for older links. Prefer updating and reading the root `docs/api/` files first.
